// GENERATED by supabase/functions/_shared/build-content.mjs — do not edit by hand.
// Every string below is extracted from the site's own source. Re-run the generator
// after changing src/pages/* or src/data/*.
export const content = {
  "generatedAt": "2026-08-28",
  "site": "https://nikhilkulkarni1755.com",
  "owner": {
    "name": "Nikhil Kulkarni",
    "headline": "AI Engineer building reproducible, scalable AI systems",
    "site": "https://nikhilkulkarni1755.com",
    "links": [
      {
        "name": "GitHub",
        "url": "https://github.com/nikhilkulkarni1755",
        "icon": "github"
      },
      {
        "name": "LinkedIn",
        "url": "https://linkedin.com/in/nikhilkulkarni1755",
        "icon": "linkedin"
      },
      {
        "name": "Email",
        "url": "mailto:nikhilkulkarni1755@gmail.com",
        "icon": "mail"
      },
      {
        "name": "Twitter",
        "url": "https://x.com/nsk1755",
        "icon": "twitter"
      }
    ]
  },
  "resume": {
    "experience": [
      {
        "title": "Software Engineer",
        "company": "Google (via Tata Consultancy Services)",
        "location": "San Jose, CA",
        "period": "June 2021 – December 2023",
        "responsibilities": [
          "To solve stakeholder analytics, built TensorFlow and MySQL chatbot with NLP and 500+ training dataset connecting to near real-time secure backend data, delivering precise insights and real-time data access to stakeholders.",
          "To facilitate data annotators, built Google Voice Command Classifier with React, Redux, and Cloud SQL. Implemented pagination and state management functionality, enabling 10,000+ voice commands to be classified.",
          "To modernize Google Search, completed 10+ Google Search Frontend Migration Projects with Typescript, HTML/CSS and Visual Regression Testing. Worked as technical liaison between client stakeholders and mgmt, delivering consistent UI/UX migrations, 200+ production code changes and 700+ code reviews."
        ]
      }
    ],
    "education": "### Bachelor of Science in Computer Science\n\nRutgers University\n\nClass of 2021",
    "skills": {
      "AI/ML": [
        "AWS Bedrock",
        "LangChain",
        "PyTorch",
        "TensorFlow",
        "Pinecone"
      ],
      "Cloud & DevOps": [
        "AWS Lambda",
        "SageMaker",
        "CloudFormation",
        "Docker",
        "Kubernetes"
      ],
      "Frontend": [
        "React",
        "TypeScript",
        "Next.js",
        "TailwindCSS",
        "Vite"
      ],
      "Backend": [
        "Node.js",
        "Python",
        "FastAPI",
        "Express",
        "PostgreSQL"
      ]
    },
    "certifications": [
      "AWS DevOps Professional",
      "AWS Developer Associate",
      "AWS Cloud Practitioner",
      "Deeplearning.AI Finetuning Large Language Models",
      "MongoDB Building RAG Apps"
    ],
    "source": "https://nikhilkulkarni1755.com/about"
  },
  "documents": [
    {
      "id": "home",
      "route": "/",
      "url": "https://nikhilkulkarni1755.com/",
      "title": "Nikhil Kulkarni — AI Engineer",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "+\n\nConnect to ClaudeWatch DemoGitHub\n\n+\n\n• min\n\nRead Article\n\n- +\n\nApp StorePlay StoreDemo\n\n# Building AI-Powered Agents in Production\n\nCreator of Iridium — a live MCP server giving AI agents real access to\nLinkedIn. Contributor to vLLM and SGLang. Impacted millions at Google Search\n(via Tata Consultancy Services), AWS DevOps Professional certified.\n\nView Resume\n\n- title: AI/ML Engineering\n- description: Building intelligent systems with AWS Bedrock, LangChain, and custom ML models\n- title: Cloud Architecture\n- description: Designing scalable solutions on AWS with serverless and containerized deployments\n- title: Full-Stack Development\n- description: Creating modern web applications with React, TypeScript, and Node.js\n\n- id: 6\n- title: Iridium\n- description: Agent-native LinkedIn access. An MCP server giving any agent live profile and post data plus real actions — comments, DMs, connection requests, scheduled posts. 50+ tools across prospecting, inbox triage, commenting, and content, with user approval required before anything sends, server-side rate limits, and a full activity log. Built and shipped end to end.\n- liveUrl: https://iridiumhqmcp.com\n- demos:\n  - label: Feature Demo\n  - src: /videos/iridium-feature-demo.mp4\n  - poster: /videos/iridium-feature-demo.jpg\n  - label: Live Demo @ WorkOS Demo Night\n  - src: /videos/iridium-live-demo.mp4\n  - poster: /videos/iridium-live-demo.jpg\n- techStack:\n  - MCP\n  - Agent Orchestration\n  - Human-in-the-Loop\n  - Tool-Calling Design\n  - OAuth\n  - Multi-Tenant\n  - FastAPI\n  - PostgreSQL\n  - Evals\n- id: 7\n- title: Iridium Agent\n- description: Open-source chat agent that drives Iridium over MCP. Two states driven by the live connection: unauthenticated it can only describe the toolset and hand back a signup link, never pretending to act; once the server reports ready, every live tool flows in automatically. That state machine is the architecture.\n- github: https://github.com/nikhilkulkarni1755/iridium-agent\n- techStack:\n  - Cloudflare Agents\n  - MCP Client\n  - Durable Objects\n  - Workers AI\n  - OAuth\n  - TypeScript\n- id: 8\n- title: vLLM on EKS\n- description: Fully observable, multi-tenant, scalable inference deployment of vLLM on EKS. Per-tenant isolation with GPU scheduling and metrics wired end to end.\n- github: https://github.com/nikhilkulkarni1755/vllm-eks-deployment\n- techStack:\n  - vLLM\n  - LLM Inference\n  - EKS\n  - Multi-Tenant\n  - Observability\n  - Terraform\n- id: 1\n- title: Linkedin Agent\n- description: Agent which will find possible connections and send them cold connection requests. I enjoy building connections in the terminal!\n- youtubeId: ZThxNcXJNPg\n- github: https://github.com/nikhilkulkarni1755/linkedin-agent\n- techStack:\n  - LangGraph\n  - LangChain\n  - Linkedin API\n  - LMStudio\n- id: 3\n- title: Skim Research Papers Agent\n- description: RAG Agent which helps summarize 100+ page research papers. Built with AWS CICD in mind.\n- youtubeId: XQVapr7IbKc\n- github: https://github.com/MacAndPC/chat-app\n- techStack:\n  - Pinecone\n  - AWS Lambda\n  - AWS S3\n  - Qwen LLM\n- id: 4\n- title: Multi Tenant Kubernetes\n- description: Kubernetes hosting system which allows each user a private secure namespace, serve a common product and host private data within namespaces.\n- youtubeId: y8u-0du76cc\n- github: https://github.com/nikhilkulkarni1755/multi-tenant-k8\n- techStack:\n  - Kubernetes\n  - Docker\n  - Prometheus\n  - Grafana\n  - Terraform\n  - AWS\n- id: 2\n- title: Terminal Tweet\n- description: Tweet from the Terminal, create a server or just tweet with the CLI\n- youtubeId: \n- github: https://github.com/nikhilkulkarni1755/twitter-oauth2\n- techStack:\n  - Uvicorn\n  - OAuth 2.0 (PKCE)\n  - X API\n- id: 5\n- title: The Progress App\n- description: iOS and Android application available for download which helps people connect with their friends and build difficult habits / skills!\n- youtubeId: \n- github: https://github.com/nikhilkulkarni1755/the-progress-app\n- techStack:\n  - React Native\n  - Firebase\n  - Websockets\n\n- id: 1\n- org: vllm-project\n- repo: vllm\n- url: https://github.com/vllm-project/vllm/pulls?q=is%3Apr+author%3Anikhilkulkarni1755\n- merged: 1\n- highlight: Fixed a Plamo2 runner crash on transformers v5, where _tied_weights_keys changed from list to dict.\n- techStack:\n  - LLM Inference\n  - GPU Serving\n  - PyTorch\n  - CI/Build\n- id: 2\n- org: sgl-project\n- repo: sglang\n- url: https://github.com/sgl-project/sglang/pull/32977\n- merged: 1\n- highlight: Preserved request priority through batched embedding requests, so scheduling order survives batching.\n- techStack:\n  - LLM Inference\n  - Batching\n  - Scheduling\n- id: 3\n- org: ax-platform\n- repo: ax-agent-studio\n- url: https://github.com/ax-platform/ax-agent-studio/pulls?q=is%3Apr+author%3Anikhilkulkarni1755\n- merged: 2\n- highlight: Built integrated auth and the monitoring dashboard (+766 lines), plus a long-polling message-format fix.\n- techStack:\n  - MCP Agents\n  - Auth\n  - Observability\n\n- id: 3\n- title: Your Agentic Coding Tool is Reading Your Secrets\n- slug: docker-secrets-injection\n- subtitle: Why your coding agent should never see your secrets — and how Docker makes that possible.\n- publishDate: 2026-05-20\n- readTime: 3\n- tags:\n  - Docker\n  - Security\n  - LLM Agents\n  - Vibe Coding\n  - DevOps\n- id: 2\n- title: From Matrices to Minds\n- slug: matmul-to-ai\n- subtitle: How a grid of numbers — multiplied together billions of times — became the engine of modern intelligence.\n- publishDate: 2026-03-05\n- readTime: 25\n- tags:\n  - Linear Algebra\n  - Neural Networks\n  - Transformers\n  - GPUs\n- id: 1\n- title: Cold Outreach Agent\n- slug: linkedin-agent\n- subtitle: Profile analysis, 6-stage LLM drafting, and reply handling — with me as the final quality gate.\n- publishDate: 2026-04-23\n- readTime: 12\n- tags:\n  - Profile Analysis\n  - LLM Pipeline\n  - Claude Sonnet\n  - PostgreSQL\n  - FastAPI"
    },
    {
      "id": "about",
      "route": "/about",
      "url": "https://nikhilkulkarni1755.com/about",
      "title": "About Nikhil Kulkarni",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "# About Me\n\nSoftware engineer building scalable Agentic AI Systems and Platforms\n\nI'm an AI Engineer building reproducible, scalable AI systems with 2+ years of experience at Google Search via Tata Consultancy Services, where I delivered 200+ production code changes. I hold a Computer Science B.S. from Rutgers University and am AWS DevOps Professional certified.\n\nI specialize in bridging Cloud infrastructure (Lambda, EKS, Bedrock) with Agentic AI systems (LangChain, RAG, Vector DBs, finetuning) to deliver production-ready AI platforms. I've also published an iOS app to the App Store, demonstrating full-stack development capabilities.\n\n## Experience\n\n- title: Software Engineer\n- company: Google (via Tata Consultancy Services)\n- location: San Jose, CA\n- period: June 2021 – December 2023\n- responsibilities:\n- To solve stakeholder analytics, built TensorFlow and MySQL chatbot with NLP and 500+ training dataset connecting to near real-time secure backend data, delivering precise insights and real-time data access to stakeholders.\n- To facilitate data annotators, built Google Voice Command Classifier with React, Redux, and Cloud SQL. Implemented pagination and state management functionality, enabling 10,000+ voice commands to be classified.\n- To modernize Google Search, completed 10+ Google Search Frontend Migration Projects with Typescript, HTML/CSS and Visual Regression Testing. Worked as technical liaison between client stakeholders and mgmt, delivering consistent UI/UX migrations, 200+ production code changes and 700+ code reviews.\n\n## Education\n\n### Bachelor of Science in Computer Science\n\nRutgers University\n\nClass of 2021\n\n## Skills\n\n- AI/ML:\n- AWS Bedrock\n- LangChain\n- PyTorch\n- TensorFlow\n- Pinecone\n- Cloud & DevOps:\n- AWS Lambda\n- SageMaker\n- CloudFormation\n- Docker\n- Kubernetes\n- Frontend:\n- React\n- TypeScript\n- Next.js\n- TailwindCSS\n- Vite\n- Backend:\n- Node.js\n- Python\n- FastAPI\n- Express\n- PostgreSQL\n\n## Certifications\n\n- AWS DevOps Professional\n- AWS Developer Associate\n- AWS Cloud Practitioner\n- Deeplearning.AI Finetuning Large Language Models\n- MongoDB Building RAG Apps\n\n## Personal Interests\n\nOutside of AI dev, I enjoy running (multiple 10Ks), playing Quadball with Silicon Valley Vipers, and reading Classic Fiction / Tech Documentation."
    },
    {
      "id": "projects",
      "route": "/projects",
      "url": "https://nikhilkulkarni1755.com/projects",
      "title": "Projects",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "# Projects\n\nA collection of my recent work in AI/ML, cloud infrastructure, and full-stack development\n\n- id: 6\n- title: Iridium\n- description: Agent-native LinkedIn access. An MCP server giving any agent live profile and post data plus real actions — comments, DMs, connection requests, scheduled posts. 50+ tools across prospecting, inbox triage, commenting, and content, with user approval required before anything sends, server-side rate limits, and a full activity log. Built and shipped end to end.\n- liveUrl: https://iridiumhqmcp.com\n- demos:\n  - label: Feature Demo\n  - src: /videos/iridium-feature-demo.mp4\n  - poster: /videos/iridium-feature-demo.jpg\n  - label: Live Demo @ WorkOS Demo Night\n  - src: /videos/iridium-live-demo.mp4\n  - poster: /videos/iridium-live-demo.jpg\n- techStack:\n  - MCP\n  - Agent Orchestration\n  - Human-in-the-Loop\n  - Tool-Calling Design\n  - OAuth\n  - Multi-Tenant\n  - FastAPI\n  - PostgreSQL\n  - Evals\n- id: 7\n- title: Iridium Agent\n- description: Open-source chat agent that drives Iridium over MCP. Two states driven by the live connection: unauthenticated it can only describe the toolset and hand back a signup link, never pretending to act; once the server reports ready, every live tool flows in automatically. That state machine is the architecture.\n- github: https://github.com/nikhilkulkarni1755/iridium-agent\n- techStack:\n  - Cloudflare Agents\n  - MCP Client\n  - Durable Objects\n  - Workers AI\n  - OAuth\n  - TypeScript\n- id: 8\n- title: vLLM on EKS\n- description: Fully observable, multi-tenant, scalable inference deployment of vLLM on EKS. Per-tenant isolation with GPU scheduling and metrics wired end to end.\n- github: https://github.com/nikhilkulkarni1755/vllm-eks-deployment\n- techStack:\n  - vLLM\n  - LLM Inference\n  - EKS\n  - Multi-Tenant\n  - Observability\n  - Terraform\n- id: 1\n- title: Linkedin Agent\n- description: Agent which will find possible connections and send them cold connection requests. I enjoy building connections in the terminal!\n- youtubeId: ZThxNcXJNPg\n- github: https://github.com/nikhilkulkarni1755/linkedin-agent\n- techStack:\n  - LangGraph\n  - LangChain\n  - Linkedin API\n  - LMStudio\n- id: 3\n- title: Skim Research Papers Agent\n- description: RAG Agent which helps summarize 100+ page research papers. Built with AWS CICD in mind.\n- youtubeId: XQVapr7IbKc\n- github: https://github.com/MacAndPC/chat-app\n- techStack:\n  - Pinecone\n  - AWS Lambda\n  - AWS S3\n  - Qwen LLM\n- id: 4\n- title: Multi Tenant Kubernetes\n- description: Kubernetes hosting system which allows each user a private secure namespace, serve a common product and host private data within namespaces.\n- youtubeId: y8u-0du76cc\n- github: https://github.com/nikhilkulkarni1755/multi-tenant-k8\n- techStack:\n  - Kubernetes\n  - Docker\n  - Prometheus\n  - Grafana\n  - Terraform\n  - AWS\n- id: 2\n- title: Terminal Tweet\n- description: Tweet from the Terminal, create a server or just tweet with the CLI\n- youtubeId: \n- github: https://github.com/nikhilkulkarni1755/twitter-oauth2\n- techStack:\n  - Uvicorn\n  - OAuth 2.0 (PKCE)\n  - X API\n- id: 5\n- title: The Progress App\n- description: iOS and Android application available for download which helps people connect with their friends and build difficult habits / skills!\n- youtubeId: \n- github: https://github.com/nikhilkulkarni1755/the-progress-app\n- techStack:\n  - React Native\n  - Firebase\n  - Websockets"
    },
    {
      "id": "blog",
      "route": "/blog",
      "url": "https://nikhilkulkarni1755.com/blog",
      "title": "Blog",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "# Blog\n\nThoughts on software engineering, AI/ML, and building products\n\n- id: 3\n- title: Your Agentic Coding Tool is Reading Your Secrets\n- slug: docker-secrets-injection\n- subtitle: Why your coding agent should never see your secrets — and how Docker makes that possible.\n- publishDate: 2026-05-20\n- readTime: 3\n- tags:\n  - Docker\n  - Security\n  - LLM Agents\n  - Vibe Coding\n  - DevOps\n- id: 2\n- title: From Matrices to Minds\n- slug: matmul-to-ai\n- subtitle: How a grid of numbers — multiplied together billions of times — became the engine of modern intelligence.\n- publishDate: 2026-03-05\n- readTime: 25\n- tags:\n  - Linear Algebra\n  - Neural Networks\n  - Transformers\n  - GPUs\n- id: 1\n- title: Cold Outreach Agent\n- slug: linkedin-agent\n- subtitle: Profile analysis, 6-stage LLM drafting, and reply handling — with me as the final quality gate.\n- publishDate: 2026-04-23\n- readTime: 12\n- tags:\n  - Profile Analysis\n  - LLM Pipeline\n  - Claude Sonnet\n  - PostgreSQL\n  - FastAPI"
    },
    {
      "id": "apps",
      "route": "/apps",
      "url": "https://nikhilkulkarni1755.com/apps",
      "title": "Apps",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "# Apps\n\nMobile and web applications I've built to solve real-world problems\n\n- id: 1\n- title: The Progress App\n- description: Build Difficult Habits with Like Minded People\n- features:\n  - In App Purchases, Websockets for amazing service\n  - End to End system designed to help build sustainable habits\n- techStack:\n  - React Native\n  - Firebase\n  - In App Purchases\n  - Websockets\n  - Node.js\n- appStoreLink: https://apps.apple.com/us/app/the-progress-app/id6503723392"
    },
    {
      "id": "privacy-policy",
      "route": "/privacy-policy",
      "url": "https://nikhilkulkarni1755.com/privacy-policy",
      "title": "Privacy Policy",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "# Privacy Policy\n\nLast Updated: December 21 2025\n\n## Overview\n\nThis is a personal portfolio website owned and operated by Nikhil Kulkarni, demonstrating\nbackend development capabilities. This privacy policy explains how I collect and handle\ninformation when you visit my site.\n\n## Information I Collect\n\nI automatically collect basic technical information when you visit this site:\n\n- IP addresses - for basic analytics and security monitoring\n- Device types - browser and operating system information\n- Page visits - which pages you view and when\n\nThis information is collected automatically through standard web server logs and helps me\nunderstand site usage patterns and demonstrate analytics capabilities.\n\n## How I Use Your Information\n\nI use this information solely for:\n\n- Site performance monitoring\n- Basic visitor analytics\n- Demonstrating backend development skills\n- Security and abuse prevention\n\nI do not:\n\n- Sell your information to anyone\n- Share your data with third parties (except my hosting provider)\n- Track you across other websites\n- Build detailed user profiles\n\n## Data Storage\n\nAll collected data is stored securely using Supabase, a cloud database service.\n\nData is retained indefinitely to support ongoing analytics and site improvement, unless you request deletion.\n\n## Your Rights\n\n### For everyone:\n\n- You can request deletion of your data by emailing me your public IP address\n- You can ask what information I have collected about your visits\n\n### For EU visitors:\n\n- You have rights under GDPR including access, deletion, and portability of your data\n\n### For California residents:\n\n- You have rights under CCPA to know about and delete your personal information\n- I do not sell personal information\n\n## Contact Me\n\nFor any privacy-related questions or to request data deletion, contact me at:\n\nEmail: nikhilkulkarni1755@gmail.com\n\nPlease include your public IP address if requesting data deletion.\n\n## Updates\n\nI may update this privacy policy occasionally. Any changes will be posted on this page\nwith a new \"Last Updated\" date.\n\nThis website is governed by the laws of California, United States."
    },
    {
      "id": "bot",
      "route": "/bot",
      "url": "https://nikhilkulkarni1755.com/bot",
      "title": "Interesting Finds Bot — crawler disclosure",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "# Interesting Finds Bot\n\nLast updated: August 28, 2026\n\n## Who operates it\n\nI'm Nikhil Kulkarni, and I operate this bot myself for my own personal site,\nnikhilkulkarni1755.com\n\n. One person's pipeline -- not a company, and not a third-party crawling service.\n\n## What it does, and why\n\nEvery day this pipeline pulls a short list of newly-launched products from public\nsources (Peerlist, Show HN, GitHub, and similar). For each one it reads the product's\nown homepage and a handful of sub-pages -- pricing, docs, about -- to check whether\nwhat it advertises is actually true, whether it solves a real problem, whether anyone\ncan use it, and whether it is agent/MCP friendly. Only products that pass get\npublished, by hand, on\nInteresting Finds\n\nwith a link back to the maker's own site. Nothing this bot reads is used to train a\nmodel.\n\n## User-Agent and how to block it\n\nSent byte-for-byte on every request this bot makes, robots.txt included:\n\n```\nInterestingFindsBot/1.0 (+https://nikhilkulkarni1755.com/bot.txt)\n```\n\nReleases bump only the version number -- the product token never changes, and a\nsecond User-Agent is never sent after a block. To keep it out entirely, add this to\nyour robots.txt:\n\n```\nUser-agent: InterestingFindsBot\nDisallow: /\n```\n\nrobots.txt is re-read on every run, so a block takes effect on the very next visit.\n\n## What it will not do\n\n- GET and HEAD requests only -- never POST, PUT, PATCH, or DELETE\n- Never logs in, signs up, checks out, or submits a form\n- Never sends a cookie, an Authorization header, or an API key\n- Never spoofs a browser User-Agent or claims to be anything else\n- At most 25 pages per site, at least 2 seconds apart\n\n## Signals it honours\n\nrobots.txt (including `Crawl-delay`), `X-Robots-Tag`, `<meta name=\"robots\">`, `Content-Signal`, `Content-Usage`, and TDM-reservation headers. When a site's own signals\nsay no, this bot does not fetch that page. Full stop.\n\n## Opt out or complain\n\nEmail\nnikhilkulkarni1755@gmail.com\n\n. Any request lands your domain on a permanent denylist, no argument -- in addition\nto, not instead of, an ordinary robots.txt block.\n\nAlso available as plain text at\n/bot.txt\n\n-- the version a script actually reads."
    },
    {
      "id": "interesting-finds",
      "route": "/interesting-finds",
      "url": "https://nikhilkulkarni1755.com/interesting-finds",
      "title": "Interesting Finds",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "# Interesting Finds\n\nLaunches I dug into and found genuinely worth your time -- checked before they\nearned a spot here, not just liked.\n\n## How a find earns its spot\n\n- label: Claim verified true\n- detail: What the maker advertises is checked against what the product actually does, not taken on faith.\n- label: Solves a rare problem\n- detail: Not another to-do list or wrapper. It addresses something most tools skip.\n- label: Anyone can use it\n- detail: No gated waitlist, no \"talk to sales\" wall. A real person can pick it up today.\n- label: Agentic / MCP friendly\n- detail: Built with agents in mind -- an API, an MCP server, or a surface an agent can actually drive.\n\n## Finds",
      "liveContent": "The list of finds on this page is read live from Supabase when the page loads, so it is not part of this corpus and its length is unknown here — this is not a statement that the list is empty. Fetch https://nikhilkulkarni1755.com/interesting-finds to see the current entries."
    },
    {
      "id": "spearfishing-fireworks-ai",
      "route": "/spearfishing/fireworks-ai",
      "url": "https://nikhilkulkarni1755.com/spearfishing/fireworks-ai",
      "title": "Fireworks AI — disaggregated vs colocated inference",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "Inference systems\n\n# A purpose-built disaggregated inference engine\n\nServing a coding agent is two jobs with opposite shapes. Reading a -token project is\ncompute-bound and happens once; writing the patch is memory-bound and happens one token at a time. Run\nthem on the same GPUs and every scheduling choice is a compromise, so I split them across two H100s and\nmeasured it against a colocated baseline on the same hardware.\n\n**Disaggregation lost, on almost every axis.**\nEverything below is that measurement — what it did, why the obvious explanation turned out to be wrong,\nand what it says about when the technique is actually the right call.\n\nserving mode:\n\nsame rig, same model — only the split changes\n\n## What the measurement actually said\n\n- **Disaggregation lost.**\nTime to first token on a -token prefix: **478ms colocated, 6,233ms\ndisaggregated**. Eight concurrent short requests: 88ms p50 against 207ms. It won one thing —\ninter-token p99, by 2.7ms.\n\n- **It was not the interconnect, which is what I expected.**\nThe engine's own counters put KV transfer at **0.75ms per request** across NVLink. The\ncache moved essentially for free. Every argument that begins \"the link between the pools is the\nfloor\" is wrong here, and I had written one before measuring.\n\n- **Two GPUs is the wrong size for this technique.**\nEach disaggregated worker runs at tensor-parallel 1; the colocated baseline runs both cards on every\nrequest at tensor-parallel 2. Splitting a two-GPU box gives up half the parallelism per request to buy\nisolation that nothing at this scale needed. That accounts for roughly 2× of a 13× gap; the rest is\norchestration around chunked prefill, which I did not profile and will not guess at.\n\n- **Sparsity buys math, not memory.**\nEach worker held **59GB** of the same weights — the arithmetic that makes two GPUs the\nminimum. A B model with B active still has to hold all of it resident.\n\n### Run it live\n\nEverything above replays measurements recorded against a real engine. This wakes that engine\nagain and reruns the same harness, so the numbers are checkable rather than merely claimed.\nIt is gated because it spends money: two H100s, torn down automatically after ten minutes.\n\n**Engine is live** on · $/hr · reclaimed in m s\n\nTear it down now\n\nPurpose-built means the workload picks the ratio\n\n\"Disaggregated\" is not a configuration you switch on and benefit from. Splitting prefill from\ndecode buys you two schedulers you can tune independently, and that is only worth the KV\ntransfer if the two halves of your traffic actually want different things. So the design\nquestion is not *whether* to split — it is what your token shape says the split should\nbe.\n\nA coding agent has an unusually lopsided shape. Every turn carries the whole project: **tokens of context** against an instruction of maybe twenty, and\nan answer of a few hundred to a few thousand. Two things follow. Prefill dominates the\narithmetic, so a naive prefill:decode ratio of 1:1 leaves the decode pool idling. And the\ncontext is *the same every turn*, which means most of that prefill should never happen\ntwice.\n\nThat is why the cache result matters more than the split result here. On this workload the\nradix tree turns a -token prefill into a lookup, and the second request's\ntime-to-first-token falls off a cliff. A purpose-built engine for coding agents is one that\ntreats the shared prefix as the primary object and sizes the prefill pool for cache\n*misses*, not for the raw token count.\n\nSerialization order is a caching decision\n\nA radix tree matches on a token sequence, so a cache hit ends at the first byte that differs.\nWhich means the order you serialize a project in quietly decides what an edit costs.\n\nThis project is sent in sorted path order. Edit `frontend/style.css`, sorted near the\nend, and **about 85% of the prefix still matches**. Edit `backend/agent.py`, sorted near the front, and roughly **97% is\ninvalidated** — the same size change, an order of magnitude apart in what it costs to\nserve. You can watch this happen in the workbench above: the header reports how much of the\nprefix survives whatever you have edited.\n\nThe useful consequence is that a coding agent should not order context alphabetically. It\nshould order it by *how likely each file is to change* — stable dependencies first,\nthe file currently being worked on last — so that the common case invalidates the smallest\npossible suffix. That is a cheap change to a context builder and it is worth real money at\nscale.\n\nThe arithmetic that decides your topology\n\nThis demo was originally specified for a single 96GB card. That is not possible, and the\nreason is worth stating plainly: **each PD worker holds a full copy of the weights.**\nPrefill and decode are different phases of the same model, not different models. Two workers\nmeans two copies.\n\nAt GB of BF16 weights that is GB before a single token of KV cache, so a\nsingle GB device cannot host both roles no matter how the flags are set. The original\ntarget — an 80B model at FP8, about 80GB per copy — needed 160GB against a 96GB card. This is\na memory-arithmetic problem wearing a configuration problem's clothes, and it is the first\nthing to check before designing anything else.\n\nSparsity buys arithmetic, not capacity\n\nThe model here holds B parameters and activates about B per token. That ratio is what makes MoE attractive for\ndecode: you are memory-bandwidth-bound, reading weights to produce one token at a time, and a\nsparse model reads far less per token.\n\nThe advantage is not symmetric across the two phases. During prefill many tokens flow through\nat once and collectively activate most of the experts anyway, so the sparsity win shrinks\nexactly where the arithmetic is heaviest — which means a sparse model changes the\nprefill:decode ratio you should be provisioning for. Decode gets cheaper per token; prefill\nlargely does not.\n\nWhere this goes next is expert placement. Experts are independent, so they can live on\ndifferent devices — which turns \"how do I split prefill from decode\" into \"how do I keep one\ndevice from becoming the hot spot when the router develops a preference\". That is a load\nbalancing problem with a learned, drifting distribution, and it is a more interesting one than\nthe PD split.\n\nI was wrong about the interconnect\n\nBefore measuring anything I argued that the link between the pools sets the floor on what\ndisaggregation can win — that these GPUs talk over PCIe, that every KV transfer pays for it,\nand that the numbers here would therefore be a floor rather than a ceiling.\n\nBoth halves were wrong. The rig reports `NV18` — eighteen NVLink connections\nbetween the two cards, plus ten InfiniBand devices. And the engine's own counters put KV\ntransfer at **0.75ms per request**, with bootstrap at another 0.73ms. Moving the\ncache is free at this scale. Whatever cost disaggregation 5.7 seconds of time-to-first-token,\nit was not the wire.\n\nLeaving that here rather than quietly deleting it, because the shape of the mistake is the\nuseful part: it is an argument that sounds right, follows from real systems knowledge, and\nwould have survived indefinitely on a page that never measured anything. The counter that\nrefuted it took one line to read.\n\nWhat it actually was: the split costs parallelism\n\nThe colocated baseline runs both H100s on every request at tensor-parallel 2. The\ndisaggregated configuration runs one card for prefill and one for decode, each at\ntensor-parallel 1. **Splitting a two-GPU box halves the compute available to any single\nrequest** in exchange for isolating the phases from each other.\n\nThat trade only pays when phase contention is what hurts — which needs enough concurrency\nthat a long prefill is genuinely blocking somebody's decode, and enough GPUs that each pool\nstill has real parallelism after the split. Eight concurrent requests on two H100s is not\nthat. There was nothing to isolate, so the split bought nothing and cost half the machine.\n\nWhich explains roughly 2× of a 13× gap. The remainder scales with prefill length — a short\nburst costs ~120ms extra, a -token prefix costs ~5.7s — and prefill is chunked\nat 8,192 tokens, so a long prompt pays whatever per-chunk orchestration exists several times\nover. I did not profile it further, so that is where the explanation stops rather than where\nthe speculation starts.\n\nScale-to-zero costs 800 seconds\n\nCold start, measured end to end: **474 seconds** to provision a machine and pull\na 21GB image, **200 seconds** to fetch 61GB of weights, **124 seconds**\nto load sixteen shards and capture CUDA graphs. Thirteen minutes from nothing to first token.\n\nThe proportions are what matter. **The part a snapshot can address is the smallest\none.** Restoring a checkpointed process replaces the 124 seconds and does nothing\nabout the 674 spent getting the image and the weights onto the machine. \"Wake in seconds\" is\nonly true once you are already paying to keep both resident — which is a warm floor, the\nexact thing scale-to-zero is supposed to avoid.\n\nThe half that does work is worth having. With `--enable-memory-saver`, releasing\nthe accelerator took **~64ms** and resuming took **~127ms**, moving\n70GB of GPU memory each way, serving again about 105ms later. But the process stayed alive\nthe whole time, so the pod stayed alive, so the bill kept running. It frees the accelerator,\nnot the rental. That makes a warm floor cheaper to hold, which given a 13-minute cold start\nis genuinely useful — and it is not scale-to-zero, and calling it that would be the kind of\nclaim this page exists to avoid.\n\nWhat I would want to measure next\n\n**One:** the same comparison at a size where the technique should win — enough\nGPUs that each pool keeps real tensor parallelism after the split, and enough concurrency that\nphase contention is actually the bottleneck. That is the experiment that would tell me whether\n13× is a property of this configuration or of my implementation, and it is the obvious next\nthing to run.\n\n**Two:** where the remaining seconds go. KV transfer is ruled out and tensor\nparallelism explains about 2×; the rest is orchestration around chunked prefill and wants a\nprofiler rather than another chart.\n\n**Three:** prefix caching over a hybrid attention model, where part of the state\nis a recurrent checkpoint rather than a KV block and the reuse rules stop being a simple tree.\nAnd **four:** what the prefill:decode ratio should be under a realistic mixture of\nagent traffic rather than three hand-picked shapes — that ratio is the actual product decision,\nand everything above is only evidence for how to choose it."
    },
    {
      "id": "take-homes-weave",
      "route": "/take-homes/weave",
      "url": "https://nikhilkulkarni1755.com/take-homes/weave",
      "title": "Weave take-home — ranking engineers from repo activity",
      "kind": "page",
      "tags": [],
      "date": null,
      "text": "back\n\nPH\n\nPostHog Engineering Impact\n\n- take-home\n- posthog/posthog\n- 90-day window\n\nScores are normalized within each bucket. See data decisions\n\nengineers ·\n\nDashboard\n\nactive\n\nThought Process\n\n- key: categories\n- label: Engineering Impact Categories\n- desc: The 7-category framework I designed\n- key: todos\n- label: Future TODOs\n- desc: What I'd build next\n- key: datafetch\n- label: Data Fetch\n- desc: Issues & decisions during collection\n\nBefore writing scoring logic, I mapped 7 distinct engineering impact archetypes. The dashboard\nimplements 3 — the ones with the strongest GitHub signal.\n\n- n: 1\n- title: Feature Owners (depth)\n- body: Own a subsystem end-to-end. Large PRs, concentrated in specific directories, long commit history in same files.\n- signal: PR size × complexity × ownership continuity\n- n: 2\n- title: Reviewers / Gatekeepers\n- body: Unblock the team. High review volume, fast turnaround, substantive comments (not just approvals).\n- signal: reviews given × comment depth × others' PRs they touched that shipped\n- n: 3\n- title: Cross-cutting Contributors\n- body: Work across many features simultaneously. Touch infra, frontend, backend in the same week.\n- signal: directory diversity × PR frequency × files changed across subsystems\n- n: 4\n- title: Bug Fixers / Reliability\n- body: Keep the product stable. Small targeted PRs, issue-linked commits, fast cycle time.\n- signal: issues closed × fix speed × recurrence rate\n- n: 5\n- title: Community Managers\n- body: PostHog is open source — a unique bucket. Triage external PRs, respond to issues, review community contributions.\n- signal: external PR reviews + issue responses + community PR merges\n- n: 6\n- title: Force Multipliers / Mentors\n- body: Their reviews make other engineers better. Impact flows through review quality.\n- signal: comment quality (length/substance), junior PRs they reviewed that shipped cleanly\n- n: 7\n- title: Infrastructure / Platform\n- body: Enable everyone else. CI/CD, tooling, build system. Low visibility but high leverage.\n- signal: files changed in .github/, Makefile, infra dirs × downstream PRs unblocked\n\nTwo high-priority improvements I'd tackle given more time.\n\n1. Keep data fresh via cron\n\nSet up a daily/weekly cron job that re-runs fetch.py, writes results to a small DB, and exposes an API endpoint. The frontend fetches from that instead of a static data.json — so rankings always reflect the last N days automatically.\n\n2. Implement more engineering categories\n\nThe 7-category framework covers far more than the current 3. Adding cross-cutting contributors, bug fixers, and community managers would give a fuller picture — especially relevant for PostHog given its open-source model.\n\nFour concrete issues hit during data collection — each with a decision and reasoning.\n\n- n: Issue 1\n- title: Fetching all PRs before date-filtering\n- problem: Initial paginate() downloaded the entire PR history before applying the 90-day filter — 6421 PRs from a repo with 37k+ total commits.\n- decision: Added a cutoff parameter to paginate() that stops at the API level as soon as an item older than the cutoff is encountered.\n- why: Early termination cuts both total API calls and runtime proportionally. For this repo, the difference is between minutes and hours.\n- n: Issue 2\n- title: 6419 PRs in 90 days = hours of runtime\n- problem: Even with early termination, ~6400 non-bot PRs in 90 days. At 0.05s sleep: 6419 × 3 × 0.05s ≈ 16 min in sleep alone, total ~5+ hours.\n- decision: Added MAX_PRS = 300 and reduced SLEEP from 0.05s → 0.02s. Cap applies when base is master and PR count exceeds 300, taking the most recent 300.\n- why: 300 PRs is sufficient to identify the top 5. 0.02s sleep keeps us under GitHub's 5000 req/hr limit. Trade-off: engineers active earlier in the window but not in the most recent 300 will be under-represented.\n- n: Issue 3\n- title: Transient HTTP 500 from GitHub API\n- problem: During the first run, one PR's /files endpoint returned HTTP 500.\n- decision: No code change needed — existing retry logic (3 attempts with backoff) handled it automatically.\n- why: 500s from GitHub are transient. If a PR's detail fetch ultimately fails, that PR contributes no file/review data but is still counted — graceful degradation.\n- n: Issue 4\n- title: PRs not filtered to main branch\n- problem: Initial API call fetched PRs targeting any base branch — feature branches, release branches, etc. This inflated PR counts with work that never lands in production.\n- decision: Added &base=master to the pulls API call. BASE_BRANCH exposed as a constant for easy adjustment.\n- why: Engineering impact should be measured by work that lands in the main codebase. Filtering to master ensures metrics reflect production-bound work only.\n\nFinal Parameters\n\n- WINDOW_DAYS\n- 90\n- Assignment spec\n- MAX_PRS\n- 300\n- Runtime constraint — sufficient for top-5 ranking\n- SLEEP_BETWEEN_CALLS\n- 0.02s\n- Safe under 5000 req/hr; keeps runtime ~10 min\n- BASE_BRANCH\n- master\n- Only PRs targeting PostHog's main branch\n- MIN_PRS_THRESHOLD\n- 3\n- Exclude engineers with too little signal\n- Retries per call\n- 3\n- Handle transient GitHub 500s/429s\n\nThought Process\n\n@\n\n#\n\nScore\n\nTop PRs\n\n#\n\nAll Metrics\n\nMost-Touched Files"
    },
    {
      "id": "blog-matmul-to-ai",
      "route": "/blog/matmul-to-ai",
      "url": "https://nikhilkulkarni1755.com/blog/matmul-to-ai",
      "title": "From Matrices to Minds",
      "kind": "post",
      "tags": [
        "Linear Algebra",
        "Neural Networks",
        "Transformers",
        "GPUs"
      ],
      "date": "2026-03-05",
      "subtitle": "How a grid of numbers — multiplied together billions of times — became the engine of modern intelligence.",
      "readTimeMinutes": 25,
      "text": "Back to Blog\n\nComplete Technical Deep-Dive\n\n# From *Matrices* to Minds\n\nHow a grid of numbers — multiplied together billions of times — became the engine of modern intelligence.\n\nAuthor: Nikhil Kulkarni, Claude Code\n\nMarch 5, 2026\n\n25 min read\n\n- Linear Algebra\n- Neural Networks\n- Transformers\n- GPUs\n\n- id: s1\n- label: 01 · Matrix Basics\n- id: s2\n- label: 02 · Matrix Multiplication\n- id: s3\n- label: 03 · Neurons & Layers\n- id: s4\n- label: 04 · CNNs\n- id: s5\n- label: 05 · RNNs & LSTMs\n- id: s6\n- label: 06 · Transformers\n- id: s7\n- label: 07 · Tokens & Embeddings\n- id: s8\n- label: 08 · GPUs & TPUs\n- id: s9\n- label: 09 · The Full Stack\n\n### Chapter 01\n\n## What is a Matrix?\n\nA matrix is just a rectangular grid of numbers — nothing more. But when you learn to multiply them, you unlock the fundamental operation behind every neural network ever built.\n\nImagine you have a 3×3 grid of numbers. Each position is called an **element**. The grid has **rows** (horizontal) and **columns** (vertical). We describe a matrix by its shape: rows × columns.\n\n#### A 3×2 Matrix · \"Shape is (3, 2)\"\n\n25−1340\n\n3 rows · 2 columns\n\nNotation: A[i][j]\n\nRow index **i** goes top→bottom\n\nColumn index **j** goes left→right\n\nSo A[0][1] = **5**\n\nAnd A[2][0] = **4**\n\nWhy do we need matrices?\n\nMatrices are a compact way to represent **linear transformations** — functions that rotate, scale, stretch, or project data. Instead of writing 100 separate equations, you write one matrix operation.\n\nIn deep learning, a matrix row is often a **data sample** and columns are **features**. A dataset of 1000 images at 28×28 pixels becomes a matrix of shape (1000, 784). Everything flows from there.\n\n### Chapter 02\n\n## Matrix Multiplication\n\nThis is *the* operation. Every layer in every neural network is fundamentally doing this one thing.\n\nThe Rule\n\nTo multiply matrix **A** (shape m×k) by matrix **B** (shape k×n), the **inner dimensions must match**. The result is shape m×n.\n\n```\nA × B = C\n(m × k) (k × n) (m × n)\n\n↑ ↑\nThese must match!\n```\n\nEach element C[i][j] is computed as the **dot product** of row i of A with column j of B:\n\n```\nC[i][j] = Σ A[i][k] × B[k][j]\nk\n\n= A[i][0]·B[0][j] + A[i][1]·B[1][j] + A[i][2]·B[2][j] + ...\n```\nStep-by-step ExampleFLOP Count — Why this matters for AI\n\nMultiplying an (m×k) matrix by a (k×n) matrix requires **m × k × n multiplications and additions** — called FLOPs (Floating Point Operations). For a single transformer layer with hidden size 4096:\n\n```\nWeight matrix shape: (4096 × 4096)\nBatch of 512 tokens: (512 × 4096)\n\nFLOPs per layer: 512 × 4096 × 4096 ≈ 8.6 Billion\nLayers in GPT-3: 96\n\nTotal FLOPs per forward pass: ~830 Billion\n```\n\nTraining GPT-3 required ~3.14 × 10²³ FLOPs. At peak A100 GPU throughput (312 TFLOPS), that would take ~32 years on a single GPU. Meta trained LLaMA-3 on ~16,000 GPUs in parallel.\n\n### Chapter 03\n\n## From Matrix Multiplication to Neural Networks\n\nA neural network is, at its core, a sequence of matrix multiplications separated by non-linear functions.\n\nThe Single Neuron\n\nOne neuron takes a vector of inputs **x**, multiplies by a weight vector **w**, adds a bias **b**, then applies an activation function **f**:\n\n```\noutput = f( w₁x₁ + w₂x₂ + w₃x₃ + b )\n= f( w·x + b )\n= f( dot_product(w, x) + b )\n```\nA Full Layer = One Matrix Multiply\n\nA layer with **n** neurons, each looking at **m** inputs, is just one matrix multiply:\n\n```\nInputs: x shape (batch_size, m) — B samples, m features each\nWeights: W shape (m, n) — m inputs → n outputs\nBias: b shape (n,)\n\nOutput = f( x @ W + b ) — shape (batch_size, n)\n\n\"@\" = matrix multiply\nf = activation (ReLU, sigmoid, etc.)\n```\nActivation Functions — The Non-Linearity\n\nWithout activation functions, stacking linear layers would still be linear (you can always combine two matrix multiplies into one). Activations are what let networks learn **curved decision boundaries**.\n\n```\nReLU(x) = max(0, x) ← most common in CNNs/MLPs\nGELU(x) ≈ x·Φ(x) ← used in GPT, BERT\nSigmoid(x) = 1 / (1 + e⁻ˣ) ← outputs 0→1, for gates\nTanh(x) = (eˣ - e⁻ˣ)/(eˣ+e⁻ˣ) ← outputs -1→1, for RNNs\nSoftmax(x) = eˣⁱ / Σeˣʲ ← for probability distributions\n```\nBackpropagation — Learning via Chain Rule\n\nNetworks learn by computing the gradient of the loss with respect to every weight, then nudging weights in the direction that reduces loss. This uses the **chain rule of calculus** applied backward through the network — hence \"backprop\".\n\n```\nForward: x → [W₁] → [ReLU] → [W₂] → [softmax] → loss L\n\nBackward: ∂L/∂W₁ = ∂L/∂y₂ · ∂y₂/∂y₁ · ∂y₁/∂W₁ (chain rule)\n\nUpdate: W₁ ← W₁ - α·∂L/∂W₁ (α = learning rate)\n\nThis backward pass is ALSO mostly matrix multiplications!\n```\n\nBoth the **forward pass** (inference) and the **backward pass** (training) are dominated by matrix multiplications. This is why GPU/TPU hardware is designed around one thing: performing massive matrix multiplies as fast as possible.\n\n### Chapter 04\n\n## Convolutional Neural Networks (CNNs)\n\nCNNs handle spatial data like images. They replace big matrix multiplies with sliding *filters* — but it's still matrix multiplication under the hood.\n\nThe Convolution Operation\n\nInstead of connecting every input pixel to every neuron (which would be enormous), a CNN uses a small **kernel/filter** (e.g., 3×3) that slides across the image, computing a dot product at each position:\n\n```\nInput image: H × W × C (height × width × channels)\nFilter/Kernel: K × K × C (usually 3×3 or 5×5)\n\nFor each position (i,j) in the output:\noutput[i][j] = sum( input[i:i+K, j:j+K] ⊙ kernel )\n↑\nelement-wise multiply, then sum\n= dot product of two vectors!\n```\n\n#### 3×3 Convolution — Edge Detection\n\nInput Patch\n\n02552550255255000\n\n⊙\n\nSobel Kernel\n\n-10+1-20+2-10+1\n\n=\n\nOutput\n\n1020\n\nStrong edge detected!\n\nConvolution is Matrix Multiplication (im2col)\n\nModern frameworks convert convolution into a matrix multiply using **im2col**: each filter application becomes a column, and you batch all of them into one big matrix multiply that GPUs love.\n\n```\n1. Reshape input patches → matrix of shape (N·H'·W', K²·C)\n2. Reshape filters → matrix of shape (K²·C, F)\n3. One matrix multiply → output of shape (N·H'·W', F)\n4. Reshape back → (N, H', W', F)\n\nThis is why convolution training is fast: it's all GEMM!\n```\nTypical CNN Architecture\n\n### Chapter 05\n\n## Recurrent Neural Networks (RNNs & LSTMs)\n\nCNNs handle space. RNNs handle *time* — sequences of data where the order matters, like text, audio, or sensor readings.\n\nThe Core Idea: Hidden State\n\nAn RNN processes one token at a time, maintaining a **hidden state** h that acts as \"memory\" of everything seen so far. At each step:\n\n```\nhₜ = tanh( Wₕₕ · hₜ₋₁ + Wₓₕ · xₜ + b )\n↑ previous ↑ current\nhidden input\n\nyₜ = Wₕ_out · hₜ ← output at step t\n\nStill just matrix multiplications!\n```\nThe Vanishing Gradient Problem\n\nRNNs struggle with **long-range dependencies**. Gradients flowing backward through many time steps get multiplied by the same weight matrix repeatedly — they either **vanish** (→ 0) or **explode** (→ ∞).\n\nLSTM: Gated Memory\n\nLSTMs (Long Short-Term Memory) add explicit **gates** to control what gets remembered, forgotten, or output. All gates are... matrix multiplications:\n\n```\nfₜ = σ( Wf · [hₜ₋₁, xₜ] + bf ) ← Forget gate: what to erase\niₜ = σ( Wi · [hₜ₋₁, xₜ] + bi ) ← Input gate: what to write\ng̃ₜ = tanh(Wg · [hₜ₋₁, xₜ] + bg) ← Candidate memory\noₜ = σ( Wo · [hₜ₋₁, xₜ] + bo ) ← Output gate: what to read\n\nCell: cₜ = fₜ ⊙ cₜ₋₁ + iₜ ⊙ g̃ₜ ← Update cell state\nHidden: hₜ = oₜ ⊙ tanh(cₜ) ← New hidden state\n\n⊙ = element-wise multiply\nσ = sigmoid (outputs between 0 and 1 = \"gate open/closed\")\n```\n\nThe **fundamental problem** with RNNs and LSTMs: they must process tokens one-at-a-time, sequentially. You can't parallelize across the sequence length. This makes them slow to train and limits context length. This is what Transformers were designed to solve.\n\n### Chapter 06\n\n## Transformers & Attention\n\nThe 2017 paper \"Attention Is All You Need\" discarded recurrence entirely. Instead, every position attends to every other position — simultaneously, in parallel — using one elegant matrix operation.\n\nSelf-Attention: The Core Mechanism\n\nFor each token, we compute three vectors by multiplying its embedding by three learned weight matrices:\n\n```\nQ = X · Wq (Queries — \"what am I looking for?\")\nK = X · Wk (Keys — \"what do I contain?\")\nV = X · Wv (Values — \"what do I output if you look at me?\")\n\nX shape: (seq_len, d_model) e.g., (512, 768)\nW shapes: (d_model, d_k) e.g., (768, 64)\n```\n\nThen attention scores are computed, scaled, softmaxed, and used to weight the values:\n\n```\nAttention(Q, K, V) = softmax( Q·Kᵀ / √d_k ) · V\n↑ ↑ ↑\ndot product scale weighted\nof all pairs sum of V\n\nQ·Kᵀ shape: (seq_len, seq_len) ← every token sees every token!\nAfter softmax: each row sums to 1 (probability distribution)\nFinal output: (seq_len, d_k) ← rich, context-aware vectors\n```\nMulti-Head Attention\n\nInstead of one attention pass, Transformers run **h heads** in parallel — each learning a different type of relationship (e.g., one head learns syntax, another learns coreference):\n\n```\nhead_i = Attention(X·Wq_i, X·Wk_i, X·Wv_i)\n\nMultiHead(X) = Concat(head_1, ..., head_h) · Wo\n\nIf d_model=768, h=12: each head d_k = 768/12 = 64\n\nTotal params per attention layer ≈ 4 × d_model²\nFor d_model=768: ≈ 2.4 million params per layer\n```\nThe Full Transformer BlockWhy Transformers Beat RNNs\n\n### Chapter 07\n\n## Tokens & Embeddings\n\nBefore any matrix can multiply, raw text must become numbers. The tokenization pipeline is the bridge between human language and matrix operations.\n\nWhat is a Token?\n\nTokens are the atomic units a model sees. They are **not** words — they are subword pieces chosen to minimize vocabulary size while handling any input. Using **Byte-Pair Encoding (BPE)**:\n\n```\n\"ChatGPT is amazing!\"\n→ [\"Chat\", \"G\", \"PT\", \" is\", \" amaz\", \"ing\", \"!\"]\n→ [ 9890, 38, 2898, 374, 16682, 278, 0 ]\n↑\ntoken IDs (integers)\n```\nEmbeddings — From ID to Vector\n\nEach token ID is looked up in an **embedding matrix** — a learned table mapping every token to a dense vector. This lookup is itself a matrix multiply (one-hot vector times embedding matrix):\n\n```\nVocabulary size: 50,257 tokens (GPT-2)\nEmbedding dim: 768 (GPT-2 small)\n\nEmbedding matrix E: shape (50257, 768)\nToken ID = 9890: → E[9890] = vector of 768 floats\n\nFull sequence of 512 tokens → matrix (512, 768)\nThis is X — the input to the first Transformer block.\n```\nPositional Encoding\n\nAttention is **permutation-invariant** — it doesn't know that token 0 comes before token 1. Positional encodings inject this order information:\n\n```\nPE[pos, 2i] = sin( pos / 10000^(2i/d_model) )\nPE[pos, 2i+1] = cos( pos / 10000^(2i/d_model) )\n\nInput to transformer = token_embedding + positional_embedding\n\nModern LLMs use RoPE (Rotary Position Embedding) instead —\nit applies rotation matrices directly inside attention,\nallowing better extrapolation to longer sequences.\n```\nFrom Token to Output Probabilitytoken IDs\n\n[9890, 38, ...]\n\nembedding lookup\n\n(vocab, d_model)\n\n+ pos encodingN × Transformer blocks\n\nattention + FFN\n\nfinal LayerNormlm_head · Wᵀ\n\n(d_model → vocab)\n\nsoftmax\n\n→ probabilities\n\nsample next token\n\nThe final projection (d_model → vocab_size) is the largest matrix multiply in the forward pass. For Llama-3 (d_model=8192, vocab=128K): that's a matrix of shape (8192, 131072) = over 1 billion parameters in a single layer.\n\n### Chapter 08\n\n## GPUs & TPUs: The Hardware Behind the Math\n\nMatrix multiplication is embarrassingly parallel. GPUs and TPUs are purpose-built to exploit this — they're not fast computers, they're fast matrix multipliers.\n\nCPU vs GPU ArchitectureTensor Cores — The Secret Weapon\n\nNVIDIA's Tensor Cores (introduced in Volta, 2017) execute a **4×4 matrix multiply-accumulate (MMA)** in a single clock cycle at mixed precision (FP16 inputs, FP32 accumulation). GPUs have thousands of these:\n\n```\nD = A × B + C\n\nA, B: (4×4) FP16 matrices\nC, D: (4×4) FP32 accumulator\n\nThis is a \"WMMA\" (Warp Matrix Multiply-Accumulate)\nOne tensor core does this in 1 clock cycle.\nH100 has 528 tensor cores × 2GHz × 16 ops/cycle\n= ~989 TFLOPS in FP16\n```\nCUDA Thread Hierarchy\n\nGPUs organize computation in a hierarchy. Understanding this explains how a single matrix multiply maps to hardware:\n\n```\nThread → executes one FMA (fused multiply-add) operation\nWarp → 32 threads that execute in lockstep (SIMT)\nBlock → many warps; share fast Shared Memory (~100 KB)\nGrid → all blocks executing the same kernel\n\nFor a matrix multiply (GEMM):\n• Each thread block computes a TILE of the output matrix\n• Tile is loaded into shared memory (fast!)\n• Inner loop multiplies the tile\n• Result written back to global GPU memory (HBM)\n```\nMemory Hierarchy & Bandwidth\n\nThe biggest bottleneck in LLM inference isn't compute — it's **memory bandwidth**. Reading weights from HBM (high-bandwidth memory) into compute units is the bottleneck:\n\n```\nLlama-3 70B model: 70 billion params × 2 bytes (FP16)\n= 140 GB just to store weights\n\nH100 HBM bandwidth: 3.35 TB/s\n\nTime to read all weights once: 140 GB / 3.35 TB/s ≈ 42ms\n\nAt 1 token/step, this is your latency floor for 1 token.\n→ This is why batching matters: same weight read, more tokens!\n```\nTPUs — Google's Dedicated Matrix Engine\n\nGoogle's **Tensor Processing Units** take specialization further. They're built entirely around matrix multiplication, sacrificing general-purpose computation for raw GEMM performance:\n\n```\nCore component: Systolic Array\n┌──────────────────────────────────────────────┐\n│ Matrix data flows through a grid of 256×256 │\n│ multiply-accumulate (MAC) units in a wave. │\n│ Each MAC unit receives partial results from │\n│ its neighbor and passes its own along. │\n│ │\n│ TPU v4: 275 TFLOPS (BF16) per chip │\n│ TPU v4 Pod: 4096 chips → ~1 ExaFLOP! │\n└──────────────────────────────────────────────┘\n\nKey advantages over GPU:\n• On-chip HBM closer to compute → less latency\n• Deterministic execution (no warp divergence)\n• Custom network topology (ICI) for pod scaling\n• 4x more energy-efficient for Transformer workloads\n```\nGPU vs TPU — The Architectural Difference\n\nThe fundamental difference is the data flow model. GPUs **pull** data from memory into independent compute blocks. TPUs **push** data through a grid of interconnected units in a wave. Run both side by side to see the contrast:\n\nPrecision Formats\n\nNot all numbers are equal in AI training. Lower precision = fewer bits = faster multiply + less memory:\n\nParallelism Strategies for Giant Models\n```\nData Parallelism — split BATCH across GPUs; same model everywhere\nGPUs sync gradients after each step (AllReduce)\n\nTensor Parallelism — split WEIGHT MATRICES across GPUs\ne.g., row 1–2048 on GPU0, row 2049–4096 on GPU1\nrequires AllReduce within every layer\n\nPipeline Parallelism — split LAYERS across GPUs\nGPU0 runs layers 1–24, GPU1 runs 25–48...\nuses micro-batching to keep GPUs busy\n\nExpert Parallelism — each GPU handles different MoE experts\nonly activates a subset of params per token\n\nGPT-3 used: 8-way tensor × 8-way pipeline × data parallelism\nrunning on 1024 A100 GPUs\n```\n\n### Chapter 09\n\n## The Full Stack: Putting it All Together\n\nEvery token you generate traces through this entire stack — from characters in a text box to numbers in a matrix to probability distributions sampled into words.\n\nOne Forward Pass, Top to Bottom**\"Paris is the capital of\"**\n\nRaw text input\n\nBPE Tokenizer → [9521, 318, 262, 3139, 286]\n\n5 token IDs\n\nEmbedding matrix lookup + positional encoding\n\n→ X of shape (5, 4096)\n\n32 × Transformer Block\n\nEach: Q=XWq, K=XWk, V=XWv → Attn(Q,K,V) → FFN → ResNorm\n\nLM head: (5, 4096) × (4096, 32000) → logits (5, 32000)Softmax → probabilities → sample last position\n\n→ token 3681 = \"France\"\n\nThe Numbers Behind \"Thinking\"\n```\nParameters: ~1 Trillion\nLayers: ~128 transformer blocks\nd_model: ~12,288\nAttention heads: ~96\nContext window: 128,000 tokens\n\nPer-token inference:\nMatrix ops: ~2T FLOPs\nMemory reads: ~2 TB from HBM\nTime on H100: ~15ms per token\n\nTraining compute: ~10²⁵ FLOPs\nTraining time: ~3 months on ~30,000 H100s\nTraining cost: ~$100 million\n```\nThe Architecture Zoo\n\nEvery model in this table — from the simplest CNN to the largest LLM — ultimately reduces to the same primitive operation: multiply two matrices, add them together, apply a non-linearity. **The miracle of deep learning is that this simple operation, composed deeply enough, with enough data, gives rise to everything from edge detection to reasoning about the world.**\n\nThe hardware race is fundamentally a race to multiply larger matrices faster. NVIDIA H100 → H200 → Blackwell B200 are all improvements in one metric: how many FP8/FP16 multiply-accumulate operations can we execute per second, and how fast can we feed them with memory bandwidth.\n\nBack to Blog\n\n▼\n\nInteractive · 2×3 times 3×2\n\nA (2×3)\n\n×\n\nB (3×2)\n\n=\n\nC (2×2)\n\nAttention Heatmap — \"The cat sat on the mat\"\n\n- The\n- cat\n- sat\n- on\n- the\n- mat\n\n- The\n- cat\n- sat\n- on\n- the\n- mat\n\nEach cell = attention weight from row-token → col-token. Darker = stronger attention.\n\nGPU — Pull Model (HBM → SRAM → Cores)\n\nHBM (High Bandwidth Memory) — 80 GB, 3.35 TB/s\n●\n\nBlock 0 · SRAM\n\nTPU — Push Model (Systolic Array)\n\n↓ partial sums exit bottom edge\n\nACC\n\n↓\n\nUnified Buffer (On-Chip SRAM) — 24 MB\n●\n\nGPU vs TPU — Side by Side"
    },
    {
      "id": "blog-linkedin-agent",
      "route": "/blog/linkedin-agent",
      "url": "https://nikhilkulkarni1755.com/blog/linkedin-agent",
      "title": "Cold Outreach Agent",
      "kind": "post",
      "tags": [
        "Profile Analysis",
        "LLM Pipeline",
        "Claude Sonnet",
        "PostgreSQL",
        "FastAPI"
      ],
      "date": "2026-04-23",
      "subtitle": "Profile analysis, 6-stage LLM drafting, and reply handling — with me as the final quality gate.",
      "readTimeMinutes": 12,
      "text": "Back to Blog\n\nEngineering Case Study\n\n# *Cold* Outreach Agent\n\nProfile analysis, 6-stage LLM drafting, and reply handling — with me as the final quality gate.\n\nAuthor: Nikhil Kulkarni\n\nApril 23, 2026\n\n12 min read\n\n- Profile Analysis\n- LLM Pipeline\n- Claude Sonnet\n- PostgreSQL\n- FastAPI\n\n- id: s1\n- label: 01 · Pipeline\n- id: s2\n- label: 02 · Tech Stack\n- id: s3\n- label: 03 · LLM Pipeline\n- id: s4\n- label: 04 · Reply Handling\n- id: s5\n- label: 05 · Scheduling\n- id: s6\n- label: 06 · Problems\n- id: s7\n- label: 07 · Results\n- id: s8\n- label: 08 · What's Next\n\nCold outreach doesn't work when it reads like cold outreach. Templates get ignored. Mass personalization tools produce messages that feel AI-generated because they are.\n\nI needed a system that generates LinkedIn connection notes indistinguishable from ones I'd write by hand — but at 10x the throughput. Each message had to reference something specific the recipient actually did, match their communication style, and pass my own review before sending.\n\nThe alternative was 30-45 minutes per person researching, writing, and scheduling. At that rate, reaching 100 people takes a month. This system does the same quality in minutes per person, with me as the final quality gate.\n\n### Section 01\n\n## Pipeline\n\n**Input:** A search query (e.g., \"Engineering Manager agentic AI\") and a target location.\n**Output:** Personalized LinkedIn connection notes, reviewed and scheduled, with reply handling via an approval workflow.\n\n```\nLinkedIn API → Search → Enrich → Generate → Review → Schedule → Send\n↓ ↓\nClaude/GPT-4 LinkedIn API\n(6-stage pipeline) (connection invites)\n\nLinkedIn Webhook\n↓\nBackground Worker\n(reply drafting, approval polling, DM sending)\n```\n\nSeven stages:\n\n- **Search** — Find profiles via keyword queries. Score on headline keywords using a three-tier weighted system.\n- **Enrich** — Fetch full profiles: work history, about, posts, comments, skills. Re-score with complete data.\n- **Generate** — Six-stage LLM pipeline produces a personalized connection note. Three attempts, best-of-N selection.\n- **Review** — Interactive CLI. Full profile context alongside the draft. Approve, edit, or trigger a rewrite. Nothing sends without my approval.\n- **Schedule** — Send time based on when the recipient is active on LinkedIn (inferred from post/comment timestamps, converted to their local timezone). Sends spread across weekdays with daily caps.\n- **Send** — Connection invites fire via LinkedIn API with gaps between sends to stay within platform limits.\n- **Reply Handling** — When someone replies, a webhook captures the message, the system drafts a response via LLM, and notifies me. I approve, edit, or request a rewrite with a single command.\n\n### Section 02\n\n## Tech Stack\n\n### Section 03\n\n## The LLM Pipeline\n\nThis is where most of the complexity lives. Each connection note goes through six stages. If quality checks fail, the pipeline retries up to three times with feedback from previous failures.\n\nStage 1\n\nProfile Intel\n\nStage 2\n\nAngle Score\n\nStage 3\n\nStyle Examples\n\nStage 4\n\nConstrained Gen\n\nStage 5\n\nLLM Detection\n\nStage 6\n\nTime Check\n\n✓ Send\n\nor retry → S4\n\n**Stage 1 — Profile Intelligence Extraction.** LLM reads the full profile and outputs structured JSON: what they're working on, their career trajectory, overlaps with my background, their communication style, and one \"resonance anchor\" — a concrete detail about their work that becomes the highlight of the message.\n\n**Stage 2 — Angle Generation and Scoring.** LLM generates 4-5 outreach angles, each scored on specificity (would this apply to 100 other people?), relevance (is there a natural reason to reach out?), and warmth (would this make them respond?). Highest-scoring angle wins.\n\n**Stage 3 — Style Example Retrieval.** The pipeline picks 2-3 reference messages from a curated set I've written. Selected by angle similarity to give the generator concrete examples of my voice.\n\n**Stage 4 — Constrained Generation.** Structural template enforces a 2-sentence format for the 300-character LinkedIn limit: what I built plus one specific thing about them, then the ask. No greeting (LinkedIn frames it), no sign-off (your name shows automatically), no links, no emojis. A banned-phrases list blocks 27+ anti-patterns including \"I'd love to connect\", \"swap notes\", \"resonated with me\", \"impressive\", \"caught my eye.\"\n\n**Stage 5 — LLM Detection Check.** A separate LLM call scores the draft 1-10 on how AI-generated it reads. Checks for formulaic structure, AI tells, unnaturally smooth transitions. Threshold: score < 7 to pass.\n\n**Stage 6 — Respect-Their-Time Check.** The LLM role-plays as the recipient and evaluates: Would you read this? Is the ask clear? Did the sender earn the right to ask? Messages with filler or unclear asks fail.\n\n**Retry logic:** If either check fails, the specific failure feeds back into Stage 4 as additional constraints. Up to 3 attempts. If none pass cleanly, the system picks the best-scoring attempt.\n\n### Section 04\n\n## Reply Handling\n\nWhen someone replies, the system drafts a contextual response, gets my approval, and sends it — without me opening LinkedIn.\n\nA webhook fires when any LinkedIn message arrives. The handler:\n\n- **Filters** — Checks the sender against tracked contacts. Messages from strangers are ignored.\n- **Deduplicates** — Checks message ID to avoid processing twice.\n- **Fetches conversation** — Pulls the full thread, not just the latest message.\n- **Drafts a reply** — Claude Sonnet gets the conversation thread, contact name, and company. System prompt: acknowledge what they said, steer toward a 15-minute call, stay under 500 characters, sound like a real person.\n- **Notifies me** with the conversation, drafted reply, and three commands:\n```\n[Name] @ [Company] replied on LinkedIn.\n\nCONVERSATION:\n[full thread with timestamps]\n\nDRAFTED REPLY:\n[LLM-generated response]\n\n---\na — send drafted reply as-is\ne, [your text] — send your version instead\nw, [feedback] — rewrite with AI and notify again\n```\n\n- **`a`** — Send the draft as-is.\n- **`e, [text]`** — Send my exact text instead.\n- **`w, [feedback]`** — Rewrite with my feedback (e.g., \"make it shorter\", \"angle should be about their k8s work\"). Notifies me again for another round.\n\n`w` is recursive — I can keep refining. The system only sends a DM on `a` or `e`. I manage LinkedIn conversations entirely from my phone.\n\n### Section 05\n\n## Activity-Pattern Scheduling\n\nEvery person's LinkedIn posts and comments have timestamps. The pipeline converts these to the recipient's local timezone and builds a histogram of activity by hour.\n\nIf >= 30% of activity falls between 5-10 PM local, they're scheduled for evening. Everyone else defaults to 9:30 AM local with jitter. Sends are restricted to weekdays. Requires a minimum of 5 data points before trusting the evening signal; below that, defaults to morning.\n\n### Section 06\n\n## Problems Encountered\n\n**LLM detection scores consistently at 8/10.** Cold outreach is structurally formulaic (compliment → credential → ask) regardless of who wrote it. The detection stage was calibrated against long-form writing, not short messages. Mitigated with best-of-N selection: three attempts give enough variance that the cleanest version surfaces.\n\n**API hangs on dense profiles.** Some profiles with extensive work histories caused the API to stall for 60+ seconds. Added explicit timeouts so failures surface cleanly instead of blocking the batch.\n\n**Tone calibration across seniority levels.** Early drafts used peer-level language with VP/Director-level recipients. The prompt now detects recipient persona (builder, enterprise leader, IC, researcher) and adjusts — reaching up to senior leaders should sound curious and humble, not like cosplaying as equals.\n\n**Bolding the wrong thing.** Initial messages bolded the sender's credentials. Reversed the rule: the highlight must be about the recipient's work — something they'll recognize and care about.\n\n**SQLite concurrent access.** The background scheduler held a database connection while HTTP endpoints tried to write. Fixed by adding timeouts to all connections so writers wait for the lock instead of failing.\n\n**Prompt iteration from verbose to minimal.** Three rounds: (1) Mode A/B with technical depth vs. trajectory approaches — too formulaic. (2) Five-angle analysis — still too abstract. (3) Final 2-sentence format with hard structural rules. This is what ships.\n\n### Section 07\n\n## Results\n\n- 100+ new connections in 2 months\n- ~25 profiles per run (discovery through drafting)\n- Average 3 LLM calls per message (generation + 2 quality checks), up to 9 on retries\n- ~15 minutes for enrichment + generation, ~10 minutes for review per batch\n- Webhook-to-notification latency under 30 seconds\n\n### Section 08\n\n## What's Next\n\n- **Autonomous agent loop.** LLM agent wrapping the CLI to run discover → enrich → draft → schedule on a daily cron with city/state rotation.\n- **A/B testing on send times.** The activity-pattern heuristic is untested against random timing.\n- **Automated follow-up DMs** after connection acceptance.\n- **Polling fallback for webhooks.** Periodic poll of recent conversations to catch missed replies.\n\nBack to Blog\n\nInteractive · 9-Stage Pipeline\n\n- id: search\n- label: 01 · Search\n- desc: Keyword query → headline-tier scoring\n- caption: Search · keyword query → headline-tier scoring\n- id: enrich\n- label: 02 · Enrich\n- desc: Full profile → re-score with complete data\n- caption: Enrich · work history, about, posts, comments → re-score with complete data\n- id: gen\n- label: 03 · Generate\n- desc: 6-stage LLM pipeline, best-of-N\n- caption: Generate · 6-stage LLM pipeline → 3 attempts → best-of-N selection\n- id: review\n- label: 04 · Review\n- desc: Interactive CLI · approve / edit / rewrite\n- caption: Review · interactive CLI → approve, edit inline, or trigger a rewrite\n- id: sched\n- label: 05 · Schedule\n- desc: Activity-pattern timing in local TZ\n- caption: Schedule · per-recipient activity histogram → evening or morning local\n- id: send\n- label: 06 · Send\n- desc: Connection invites via LinkedIn API\n- caption: Send · connection invites fire with gaps to stay within platform limits\n- id: reply\n- label: 07 · Reply Handling\n- desc: Webhook → LLM draft → approval → DM\n- caption: Reply Handling · webhook → LLM draft → notification → approval poll → DM\n\nInteractive · Reply Approval Flow\n\nMy email reply (a / e / w)\n\na\n\napprove\n\ne\n\nedit\n\nw\n\nrewrite\n\nw is recursive — keeps cycling LLM Draft → Notification until I send a or e.\n\n↺ rewrite loop"
    },
    {
      "id": "blog-docker-secrets-injection",
      "route": "/blog/docker-secrets-injection",
      "url": "https://nikhilkulkarni1755.com/blog/docker-secrets-injection",
      "title": "Your Agentic Coding Tool is Reading Your Secrets",
      "kind": "post",
      "tags": [
        "Docker",
        "Security",
        "LLM Agents",
        "Vibe Coding",
        "DevOps"
      ],
      "date": "2026-05-20",
      "subtitle": "Why your coding agent should never see your secrets — and how Docker makes that possible.",
      "readTimeMinutes": 3,
      "text": "Back to Blog\n\nAgentic AI Engineering\n\n# Your Agentic Coding Tool is *Reading* Your Secrets\n\nWhy your coding agent should never see your secrets — and how Docker makes that possible.\n\nAuthor: Nikhil Kulkarni\n\nMay 20, 2026\n\n3 min read\n\n- Docker\n- Security\n- LLM Agents\n- Vibe Coding\n- DevOps\n\n- id: s1\n- label: 01 · The Problem\n- id: s2\n- label: 02 · The Fix\n- id: s3\n- label: 03 · Caveats\n\nEvery session with an agentic coding tool — Claude Code, Cursor, Copilot, all of them — is systematically insecure in a way most developers haven't thought through.\n\nWhen these tools explore your codebase, every file they read gets sent to the provider's servers as context. That includes your `.env` file, hardcoded VM IPs, internal URLs, API keys, and passwords.\n\n### Section 01\n\n## The Problem\n\nThey do this silently inside large toolcalls using standard shell commands like `cat`, `grep -r`, `find`, and `printenv` — completely normal parts of how coding agents navigate a project. Some of this data can be rotated if it leaks: a new API key takes thirty seconds. But some of it can't — your server IP, your personal information, your internal infrastructure. These live in your codebase because your application needs them. There's no reason they need to be stored on a third-party AI provider's servers indefinitely.\n\n### Section 02\n\n## The Fix\n\nThe solution is simpler than it sounds, and you don't need anything beyond Docker Desktop or OrbStack. Run your coding agent inside a Docker container and inject all sensitive values as environment variables at container launch via `--env-file` — a file that lives entirely outside your codebase. The codebase the agent works on is completely clean: no real secrets, no hardcoded values, nothing to find and transmit. The agent still has full access to do everything it needs — SSH into a server, authenticate with an API, connect to a database — because the values exist as process-level environment variables, not as files on disk.\n\n```\n# secrets.env lives outside the repo, never committed\ndocker run \\\n--env-file /path/outside/repo/secrets.env \\\n--mount type=bind,source=$(pwd),target=/workspace \\\nmy-agent-image\n\n# Agent references names, never values:\n# ssh ${VM_IP} ← not ssh 192.168.1.1\n# curl -H \"Authorization: ${API_KEY}\" ← not curl -H \"Authorization: sk-abc123\"\n# psql ${DB_URL} ← not psql postgresql://user:pass@host/db\n```\n\nCritically, the container uses a bind mount, meaning the agent edits the exact same files on your host machine in real time: your `PLAN.md` gets updated, your code changes persist, your `CLAUDE.md` and memory files are all live and valid across sessions. Nothing is lost, nothing is copied — the container and your local machine share the same filesystem.\n\nCorporations already do a higher-level version of this: secrets are injected at runtime from vaults like AWS Secrets Manager or HashiCorp Vault, scoped per service, never stored as files anywhere. For vibecoders, the Docker `--env-file` approach is the right level of this same thoughtfulness.\n\n### Section 03\n\n## Caveats\n\nThis setup is not a silver bullet, and knowing where it breaks is as important as knowing how it works. The biggest surface-level gap: direct commands like `printenv`, `env`, and `set` dump everything injected into the process. Block these explicitly in `.claude/settings.json` using Claude Code's native permissions system:\n\n```\n{\n\"permissions\": [\n{ \"type\": \"deny\", \"tool\": \"Bash(printenv*)\" },\n{ \"type\": \"deny\", \"tool\": \"Bash(env*)\" },\n{ \"type\": \"deny\", \"tool\": \"Bash(set*)\" }\n]\n}\n```\n\nYou can reinforce this with a rule in `CLAUDE.md` instructing the agent never to print environment variable values — but the `settings.json` block is the enforceable one. The deeper gap is harder to close: if the agent writes or runs a script that logs its own environment to stdout — a Python script with `print(os.environ.items())`, a Node script with `console.log(process.env)` — that output comes back as a tool result, lands in the model's context, and reaches Anthropic's servers anyway. The Docker boundary doesn't help here.\n\n**The deny rules reduce the risk for direct shell commands, but can't intercept subprocess stdout.** This approach meaningfully shrinks the attack surface; it doesn't eliminate it entirely.\n\nFor a tighter boundary, Claude Code's native sensitive config and PreToolUse hooks (see: *nopeek*) can scrub matched patterns before they ever reach the model — worth researching if you need the next level. The second caveat is simpler: don't run your agent in auto-accept mode as a habit. Confirmation prompts exist for a reason. Treat them as a feature.\n\nThe extra hour of setup is the difference between your infrastructure living on someone else's servers forever and it never being there at all.\n\nBack to Blog\n\nsecret exposure — two scenarios\n\n✗ hardcoded / grepped\n\n$ grep -r \"password\" .\n\n$ cat .env\n\nDB_PASS=s3cr3t!\n\nAPI_KEY=sk-abc123\n\nVM_IP=192.168.1.1\n\n↑ sent to LLM servers\n\nstored forever\n\n✓ injected\n\ndocker run \\\n--env-file secrets.env\n\nssh ${VM_IP}\n\ncurl -H \"key: ${API_KEY}\"\n\npsql ${DB_URL}\n\n⟳ LLM sees names only\n\nvalues stay local\n\nunsafe pathsafe path\n\ninjection flow — secrets never in the repo\n\nsecrets.env\n\noutside repo\n\n→\n\ndocker run\n\n--env-file\n\n→\n\ncontainer\n\nprocess env vars\n\n↓\n\n${API_KEY}\n\n${DB_URL}\n\n${VM_IP}\n\n↓\n\nagent ✓\n\nsees names only\n\n→\n\nLLM prompt\n\nno real values\n\n→\n\nAnthropic / OpenAI\n\nharmless log\n\nbind mount keeps host files in sync — edits persist, nothing is copied"
    }
  ],
  "projects": [
    {
      "id": 6,
      "title": "Iridium",
      "description": "Agent-native LinkedIn access. An MCP server giving any agent live profile and post data plus real actions — comments, DMs, connection requests, scheduled posts. 50+ tools across prospecting, inbox triage, commenting, and content, with user approval required before anything sends, server-side rate limits, and a full activity log. Built and shipped end to end.",
      "liveUrl": "https://iridiumhqmcp.com",
      "demos": [
        {
          "label": "Feature Demo",
          "src": "/videos/iridium-feature-demo.mp4",
          "poster": "/videos/iridium-feature-demo.jpg"
        },
        {
          "label": "Live Demo @ WorkOS Demo Night",
          "src": "/videos/iridium-live-demo.mp4",
          "poster": "/videos/iridium-live-demo.jpg"
        }
      ],
      "techStack": [
        "MCP",
        "Agent Orchestration",
        "Human-in-the-Loop",
        "Tool-Calling Design",
        "OAuth",
        "Multi-Tenant",
        "FastAPI",
        "PostgreSQL",
        "Evals"
      ]
    },
    {
      "id": 7,
      "title": "Iridium Agent",
      "description": "Open-source chat agent that drives Iridium over MCP. Two states driven by the live connection: unauthenticated it can only describe the toolset and hand back a signup link, never pretending to act; once the server reports ready, every live tool flows in automatically. That state machine is the architecture.",
      "github": "https://github.com/nikhilkulkarni1755/iridium-agent",
      "techStack": [
        "Cloudflare Agents",
        "MCP Client",
        "Durable Objects",
        "Workers AI",
        "OAuth",
        "TypeScript"
      ]
    },
    {
      "id": 8,
      "title": "vLLM on EKS",
      "description": "Fully observable, multi-tenant, scalable inference deployment of vLLM on EKS. Per-tenant isolation with GPU scheduling and metrics wired end to end.",
      "github": "https://github.com/nikhilkulkarni1755/vllm-eks-deployment",
      "techStack": [
        "vLLM",
        "LLM Inference",
        "EKS",
        "Multi-Tenant",
        "Observability",
        "Terraform"
      ]
    },
    {
      "id": 1,
      "title": "Linkedin Agent",
      "description": "Agent which will find possible connections and send them cold connection requests. I enjoy building connections in the terminal!",
      "youtubeId": "ZThxNcXJNPg",
      "github": "https://github.com/nikhilkulkarni1755/linkedin-agent",
      "techStack": [
        "LangGraph",
        "LangChain",
        "Linkedin API",
        "LMStudio"
      ]
    },
    {
      "id": 3,
      "title": "Skim Research Papers Agent",
      "description": "RAG Agent which helps summarize 100+ page research papers. Built with AWS CICD in mind.",
      "youtubeId": "XQVapr7IbKc",
      "github": "https://github.com/MacAndPC/chat-app",
      "techStack": [
        "Pinecone",
        "AWS Lambda",
        "AWS S3",
        "Qwen LLM"
      ]
    },
    {
      "id": 4,
      "title": "Multi Tenant Kubernetes",
      "description": "Kubernetes hosting system which allows each user a private secure namespace, serve a common product and host private data within namespaces.",
      "youtubeId": "y8u-0du76cc",
      "github": "https://github.com/nikhilkulkarni1755/multi-tenant-k8",
      "techStack": [
        "Kubernetes",
        "Docker",
        "Prometheus",
        "Grafana",
        "Terraform",
        "AWS"
      ]
    },
    {
      "id": 2,
      "title": "Terminal Tweet",
      "description": "Tweet from the Terminal, create a server or just tweet with the CLI",
      "youtubeId": "",
      "github": "https://github.com/nikhilkulkarni1755/twitter-oauth2",
      "techStack": [
        "Uvicorn",
        "OAuth 2.0 (PKCE)",
        "X API"
      ]
    },
    {
      "id": 5,
      "title": "The Progress App",
      "description": "iOS and Android application available for download which helps people connect with their friends and build difficult habits / skills!",
      "youtubeId": "",
      "github": "https://github.com/nikhilkulkarni1755/the-progress-app",
      "techStack": [
        "React Native",
        "Firebase",
        "Websockets"
      ]
    }
  ],
  "contributions": [
    {
      "id": 1,
      "org": "vllm-project",
      "repo": "vllm",
      "url": "https://github.com/vllm-project/vllm/pulls?q=is%3Apr+author%3Anikhilkulkarni1755",
      "merged": 1,
      "highlight": "Fixed a Plamo2 runner crash on transformers v5, where _tied_weights_keys changed from list to dict.",
      "techStack": [
        "LLM Inference",
        "GPU Serving",
        "PyTorch",
        "CI/Build"
      ]
    },
    {
      "id": 2,
      "org": "sgl-project",
      "repo": "sglang",
      "url": "https://github.com/sgl-project/sglang/pull/32977",
      "merged": 1,
      "highlight": "Preserved request priority through batched embedding requests, so scheduling order survives batching.",
      "techStack": [
        "LLM Inference",
        "Batching",
        "Scheduling"
      ]
    },
    {
      "id": 3,
      "org": "ax-platform",
      "repo": "ax-agent-studio",
      "url": "https://github.com/ax-platform/ax-agent-studio/pulls?q=is%3Apr+author%3Anikhilkulkarni1755",
      "merged": 2,
      "highlight": "Built integrated auth and the monitoring dashboard (+766 lines), plus a long-polling message-format fix.",
      "techStack": [
        "MCP Agents",
        "Auth",
        "Observability"
      ]
    }
  ],
  "apps": [
    {
      "id": 1,
      "title": "The Progress App",
      "description": "Build Difficult Habits with Like Minded People",
      "features": [
        "In App Purchases, Websockets for amazing service",
        "End to End system designed to help build sustainable habits"
      ],
      "techStack": [
        "React Native",
        "Firebase",
        "In App Purchases",
        "Websockets",
        "Node.js"
      ],
      "appStoreLink": "https://apps.apple.com/us/app/the-progress-app/id6503723392",
      "images": []
    }
  ],
  "posts": [
    {
      "id": 3,
      "title": "Your Agentic Coding Tool is Reading Your Secrets",
      "slug": "docker-secrets-injection",
      "subtitle": "Why your coding agent should never see your secrets — and how Docker makes that possible.",
      "publishDate": "2026-05-20",
      "readTime": 3,
      "tags": [
        "Docker",
        "Security",
        "LLM Agents",
        "Vibe Coding",
        "DevOps"
      ],
      "route": "/blog/docker-secrets-injection",
      "url": "https://nikhilkulkarni1755.com/blog/docker-secrets-injection"
    },
    {
      "id": 2,
      "title": "From Matrices to Minds",
      "slug": "matmul-to-ai",
      "subtitle": "How a grid of numbers — multiplied together billions of times — became the engine of modern intelligence.",
      "publishDate": "2026-03-05",
      "readTime": 25,
      "tags": [
        "Linear Algebra",
        "Neural Networks",
        "Transformers",
        "GPUs"
      ],
      "route": "/blog/matmul-to-ai",
      "url": "https://nikhilkulkarni1755.com/blog/matmul-to-ai"
    },
    {
      "id": 1,
      "title": "Cold Outreach Agent",
      "slug": "linkedin-agent",
      "subtitle": "Profile analysis, 6-stage LLM drafting, and reply handling — with me as the final quality gate.",
      "publishDate": "2026-04-23",
      "readTime": 12,
      "tags": [
        "Profile Analysis",
        "LLM Pipeline",
        "Claude Sonnet",
        "PostgreSQL",
        "FastAPI"
      ],
      "route": "/blog/linkedin-agent",
      "url": "https://nikhilkulkarni1755.com/blog/linkedin-agent"
    }
  ]
} as const;
export type Document = (typeof content.documents)[number];
